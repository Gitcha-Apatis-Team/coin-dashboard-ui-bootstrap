import { TickerCurrencyPipe } from './ticker-currency.pipe';

describe('TickerCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TickerCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
